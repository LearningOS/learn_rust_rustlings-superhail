// variables2.rs
// Make me compile! Execute the command `rustlings hint variables2` if you want a hint :)


// rust variables are created in runtime, while consts are created in compile-time
fn main() {
    let x: i32 = 10;
    if x == 10 {
        println!("Ten!");
    } else {
        println!("Not ten!");
    }
}
